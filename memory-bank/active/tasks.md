# Task: href_decorator rules-based config redesign

* Task ID: href-decorator-rules
* Complexity: Level 2
* Type: Simple Enhancement

## Implementation Plan

1. Refactor `_plugins/10_href_decorator.rb`: rules-based config, collection filtering — **done**
2. Update `_config.yaml`: new `defaults` + `rules` schema — **done**
3. Verify build and behavior — **done**

## Status

- [x] Implementation complete
- [x] Build verified (bundle exec jekyll build)
- [x] QA (PASS)
- [x] Reflect
