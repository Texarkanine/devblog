PASS WITH ADVISORY

TDD Plan Encoding: PASS. Unit 1 (post footer byline) is executable and places inspect-red before production code. Operator waived a test suite; build+HTML inspect is the encoded verification, not a preamble disclaimer. Unit 2 is prose/policy and owes no tests. No change-detector steps to strike; no test/production order swap needed.

Convention Compliance: PASS. Touch list is `_layouts/post.html` only. The flex `space-between` row copies the existing post header. Date rendering uses `site.theme_config.date_format` with the same `"%Y-%m-%d"` default as the header and garden. `collection_dates.rb` and `_layouts/garden.html` stay untouched, matching the reuse/constraint decisions.

Dependency Impact: PASS. Every document with `layout: post` will pick up the footer. Tag/category archives, `default.html`'s site-level footer, garden pages, and the dates generator are not consumers of this markup and are already excluded. `last_modified` is already assigned on collection docs.

Conflict Detection: PASS. Posts have no last-updated byline today. Garden's "Last tended:" is the same field with different copy and placement; the plan does not duplicate that UI. The generator comment already anticipates a "last updated" display. No public contract change.

Completeness: PASS. Brief requirements 1–3 and the six listed behaviors map to a concrete Liquid condition and footer sibling after the tags `{% endif %}`. This-run fixture check: `blog/diary/_posts/2026-03-13-seven-weeks.md` last commit is 2026-03-13 (same calendar day); `blog/guide/_posts/2026-03-14-adeptus-mechanicus-bootcamp-gentle-seduction.md` last commit is 2026-08-22 (later day). The inspect needle `last updated` does not appear in post bodies.

Advisory — live fixtures missing for three behaviors: no post lacks tags (0 untagged posts); earlier-day and missing `last_modified` will not appear in a normal build because the generator always sets the field. Those three are verified by reading the Liquid guards, not by inspecting a real page.

Advisory — timezone is unset in `_config.yaml`. Calendar-day compare uses the build environment's zone. Pinning `timezone` would make byline visibility identical on WSL, CI, and App Platform. Not required to start build.

Advisory — radical innovation (not applied): after assigning `last_modified`, also set `last_modified_at` to the same Time unless front matter already has it. `jekyll-seo-tag` and `jekyll-sitemap` already read `last_modified_at`; today they only see the publish date. Search results would then match the footer byline. Leave the plan as-is unless the operator wants that extra line in the generator.
