PASS WITH ADVISORY

Prerequisites: Level 2 plan is complete. No creative-phase docs required. Implementation units, inspect fixtures, and operator constraints are present.

TDD plan encoding: Unit 1 (Body-aware last_modified — executable) places inspect-red before production code (steps 1 and 3, then write-code in step 4). Operator waived an automated suite; inspect-red is the test step. No change-detector steps to strike. Unit 2 is prose/policy (build + inspect) and owes no tests. Not FAIL (blocking): inventing a harness would violate "Do not invent tests" and the project-brief constraint.

Convention: Change stays in `_plugins/collection_dates.rb`. New methods `extract_markdown_body` / `git_last_body_commit_date` match existing `git_*_commit_date` naming, `Shellwords`, `%ai`, and `Time.parse`. Layouts stay untouched. Generator already walks every collection; this site only has `posts` and `garden`.

Dependency: `last_modified` is consumed only by `_layouts/post.html` (same-day hide) and `_layouts/garden.html` (always show). No other Liquid. No live `last_modified:` front-matter overrides exist; the existing `unless doc.data.key?("last_modified")` guard is the override path and is kept.

Conflict: No existing body-extract helper. This intentionally reverses the prior post-last-updated-byline assumption that summary-only commits count. `jekyll-seo-tag` / `jekyll-sitemap` still do not read `last_modified` (they want `last_modified_at`); same out-of-scope gap as the prior task.

Completeness: All four brief requirements have a concrete path (one generator change, layouts unchanged, override guard kept). Named inspect fixtures were re-checked against git history and match the plan (you-cant-hide last-body 2025-12-07; stop-doing-agents 2026-02-24; Mechanicus both 2026-08-22; 1900s-computer-gaming last-body 2025-12-02). elgato-alias is `blog/diary/_posts/2026-04-19-elgato-alias.md` (single commit, same day as publish). Four garden pages have body-day equal to first-commit day (`do-llms-understand-for-each`, `general-purpose-industry-refining-tool`, `nuclear-pyramid`, `stories`); none of the four date-moving garden pages are same-day-as-planted.

Advisories:
1. Garden same-day inspect target is unnamed. Pick any of the four above (e.g. `_garden/stories.md`); they do not move Last tended, they only guard "still shown."
2. `git show #{hash}:#{path}` should reuse the existing relative-path + Shellwords pattern from `git_last_commit_date`, including `2>/dev/null` and a `.git` guard. The plan implies this but does not spell the path normalization.
3. `last_modified_at` remains unset, so sitemap/SEO lastmod can still reflect last-commit or build time after the footer becomes body-aware.

Radical innovation (advisory, not applied): In `generate`, set `doc.data["last_modified_at"]` to the same body-aware Time unless already present, so jekyll-sitemap lastmod and jekyll-seo-tag dateModified match the footer/Last tended. This is the moment the date source becomes truthful; wiring the third surface now avoids a three-way split. Cost is a one-line assignment plus a sitemap/SEO inspect. Not required for this brief.
