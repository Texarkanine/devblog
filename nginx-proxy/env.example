# Copy to .env and fill in your values
UPSTREAM_HOST=your-app.ondigitalocean.app
APP_NAME=myapp

# Optional: Path prefix if your site is hosted at a subpath (e.g., /abc123)
# If set, all requests will be prefixed with this path when proxying
# Examples: /abc123, /uuid-here, /site-path
# Leave empty or omit if site is at root
UPSTREAM_PATH=

# Optional: Host header to send to upstream
# If not set, uses original request host ($host)
# If upstream requires specific Host header (e.g., for 403 errors), set this
# Examples: 
#   - Leave unset: Uses original request domain (flexible, works if upstream accepts it)
#   - Set to UPSTREAM_HOST: Matches upstream domain (required if upstream validates Host header)
#   - Set to specific domain: Any domain the upstream expects
UPSTREAM_HOST_HEADER=
